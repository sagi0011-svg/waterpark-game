# אפיון פיצ'ר: הדרכה דינאמית + מילון מונחים (מסמך למימוש ב-Claude Code)

> **פרויקט:** ספלאש בלאגן — סימולציית פארק מים ישראלי
> **מה בונים:** (א) סיור פותח קצר עם הדגשת אלמנטים וחץ, (ב) טיפים בהקשר, (ג) מילון מונחים שמסביר מה מעלה ומה מוריד כל מדד.
> **סטטוס:** המשחק כבר עובד ופרוס. זו תוספת על גבי קוד קיים — **אין לשבור זרימות קיימות**.
> **גרסה:** 2 (מעודכן: כל השלבים מתקדמים בכפתור "הבא", 4 שלבים בסיור, נוסף מילון מונחים)

---

## 0. הנחיה לקלוד קוד (TL;DR)

בנה מערכת הדרכה גנרית ("Tour Engine") שמדגישה אלמנט אחד במסך, מחשיכה את השאר, ומציגה בועה עם חץ שמצביע על האלמנט. בנוסף — מסך "מילון מונחים" שמסביר לשחקן מה **מעלה** ומה **מוריד** כל מדד במשחק.

עקרונות מחייבים:
1. **מבוסס נתונים** — כל התוכן (שלבי סיור, טיפים, מילון) יושב בקבצי קונפיגורציה נפרדים. הוספת תוכן = הוספת אובייקט למערך, בלי לגעת בלוגיקה.
2. **חיבור לאלמנטים דרך `data-tour="..."`** בלבד — לא לפי CSS class ולא לפי מבנה DOM.
3. **RTL מלא** — מיקומים, חצים ואנימציות חייבים לעבוד נכון בעברית.
4. **Mobile first** — הבועה לא יוצאת מהמסך, לא נחתכת, ולא מסתירה את האלמנט שהיא מסבירה עליו.
5. **בלי ספריות חיצוניות** אם אפשר. אם בכל זאת — `driver.js` (קלה, תומכת RTL). אין להשתמש במשהו שדורש jQuery.
6. **מספרים לא מועתקים ידנית** — אם קיימים במשחק קבועים (מכפילי עונתיות, משקלי שביעות רצון, עלויות), המילון והטיפים מייבאים מהם. אין להקשיח מספרים כפולים שיתפרקו כשהאיזון ישתנה.

---

## 1. מודל ההדרכה — היברידי (שתי שכבות)

### שכבה א' — הסיור הפותח
מופעל **פעם אחת**, מיד אחרי שהשחקן מסיים את מסך ההגדרות (שם מנהל → שם פארק → מטרה → קושי → קצב), ולפני שהחודש הראשון מתחיל.
- **4 שלבים בלבד.** קצר במכוון.
- **המשחק מוקפא** בזמן הסיור.
- כל השלבים מתקדמים בכפתור "הבא ➡️" (ראה סעיף 3).

### שכבה ב' — טיפים בהקשר
טיפ בודד שקופץ **בפעם הראשונה** שפיצ'ר מסוים מופיע בפועל. כל מה שלא נכנס ל-4 השלבים עובר לכאן (סעיף 7).

---

## 2. שליטת השחקן

| יכולת | התנהגות |
|---|---|
| **התקדמות** | כפתור "הבא ➡️". בשלב האחרון הכפתור הופך ל"יאללה, בוא נתחיל 🏊". |
| **חזרה אחורה** | כפתור "◀️ הקודם", מוצג מהשלב השני ואילך. |
| **דילוג על הכול** | קישור טקסט קטן "דלג על ההדרכה" בפינת הבועה, זמין **בכל שלב**. לחיצה → דיאלוג אישור קצר ("בטוח? תמיד אפשר להחזיר מכפתור ה-❓") → סוגר הכול ומסמן את כל השלבים כנצפו. |
| **ביטול טיפים** | בדיאלוג האישור יש גם צ'קבוקס: "אל תציג לי טיפים בהמשך המשחק". |
| **הפעלה מחדש** | כפתור **❓** קבוע בהדר העליון. לחיצה פותחת תפריט: `[▶️ הפעל את הסיור מחדש]` `[📖 מילון מונחים]`. עובד בכל שלב במשחק, גם בחודש 9. |
| **מתי מופעל אוטומטית** | בכל **משחק חדש**, אלא אם השחקן סימן "אל תציג שוב" — אז רק דרך ❓. |

---

## 3. התקדמות בין שלבים

**בגרסה זו: כל השלבים מתקדמים בכפתור "הבא" בלבד.** אין שלבים שדורשים לחיצה על האלמנט המודגש.

מכאן נובע:
- ה-Overlay חוסם קליקים על **כל** המסך. האלמנט המודגש מואר אבל **לא לחיץ**.
- אין צורך בטיימאאוטים, ב-`hint`, או בטיפול ב"השחקן נתקע".
- השדה `advanceOn` בכל זאת קיים במבנה הנתונים עם הערך `'next'` בלבד — כדי לאפשר בעתיד שלבים אינטראקטיביים בלי לשכתב את ה-Engine. **אין לממש כרגע ערכים אחרים.**

**מקלדת:** `Enter` / `→` = הבא · `←` = הקודם · `Esc` = דיאלוג דילוג.

---

## 4. מפרט ויזואלי — Spotlight + חץ + בועה

```
┌─────────────────────────────────────┐
│ ████████████████████████████████████ │  ← Overlay שחור 75%
│ ████████  ╔═══════════════╗  ██████ │
│ ████████  ║  💰 ₪250,000  ║  ██████ │  ← "חור" מואר סביב האלמנט
│ ████████  ╚═══════════════╝  ██████ │
│ ██████████████  ▲  ███████████████── │  ← חץ שנוגע בקצה הבועה
│ ██████  ┌────────────────────┐ ████ │
│ ██████  │ 💰 הכסף שלך         │ ████ │
│ ██████  │ כל שקל נחשב. מינוס  │ ████ │
│ ██████  │ = הפארק נסגר.       │ ████ │
│ ██████  │ ↑ כרטיסים, מזנונים  │ ████ │  ← בלוק "מה מעלה / מה מוריד"
│ ██████  │ ↓ תחזוקה, קנסות     │ ████ │
│ ██████  │  1/4   [דלג][הבא ➡️]│ ████ │
│ ██████  └────────────────────┘ ████ │
└─────────────────────────────────────┘
```

### 4.1 שכבת ה-Overlay
- `position: fixed; inset: 0; z-index: 9998;` רקע `rgba(0,0,0,0.75)`.
- **מימוש ה"חור":** אלמנט ריק שממוקם בדיוק מעל ה-bounding box של היעד, עם `box-shadow: 0 0 0 9999px rgba(0,0,0,0.75)`. זו השיטה הפשוטה והחלקה — **לא** לחתוך DOM ולא לשכפל אלמנטים.
- `border-radius` תואם לאלמנט (ברירת מחדל `12px`), `padding` 8px מסביב, `transition: all 300ms ease` — כך שהזרקור "זוחל" בין שלבים במקום לקפוץ.
- טבעת פועמת עדינה סביב החור בתכלת המשחק (`#0b84d6`).

### 4.2 החץ
- משולש CSS או SVG **בצבע רקע הבועה**, כך שנראה כהמשך שלה.
- ממוקם לפי `placement` (`top` / `bottom` / `right` / `left`) ו**תמיד מצביע לכיוון האלמנט**.
- מיושר למרכז האלמנט, אך "נצמד" בגבול 16px מקצות הבועה כדי שלא יחרוג.
- אנימציית ריחוף עדינה (3px, 1.2s לולאה).

### 4.3 בועת ההסבר
- כרטיס בסגנון המשחק: רקע כהה (~`#161b22`), מסגרת דקה בהירה, `border-radius: 16px`, צל רך.
- רוחב מקסימלי `320px`; בנייד `calc(100vw - 32px)`.
- מבנה פנימי מלמעלה למטה:
  1. **אייקון + כותרת** (שורה אחת, מודגש)
  2. **גוף** — 1–2 משפטים, בטון ההומור של המשחק
  3. **בלוק אפקטים** (אופציונלי, ראה 4.5)
  4. **פוטר** — מונה שלבים משמאל (`2/4`) + כפתורים מימין
- **אלגוריתם מיקום:** ניסיון לפי הסדר `bottom → top → left → right`; נבחר הראשון שנכנס במלואו ב-viewport. אם אף אחד לא נכנס — הבועה ממורכזת בתחתית המסך והחץ מוסתר.

### 4.4 מובייל / Bottom Sheet
- כשגובה המסך < 600px, או כשהיעד תופס יותר מ-40% מהמסך: הבועה עוברת אוטומטית ל**Bottom Sheet** (נצמדת לתחתית, רוחב מלא) — דפוס שכבר קיים במשחק. החץ מוסתר.
- לפני כל שלב: `scrollIntoView({ block: 'center', behavior: 'smooth' })` על היעד, ואז להמתין ל-`requestAnimationFrame` + ~350ms **לפני** חישוב המיקום. אחרת הבועה תמוקם לפי מיקום ישן.

### 4.5 בלוק "מה מעלה / מה מוריד" (חדש — חשוב)
בכל שלב שמסביר **מדד** (כסף, מבקרים, שביעות רצון, דירוג), הבועה מציגה מתחת לטקסט שתי שורות קומפקטיות:

```
↑  כרטיסים · מזנונים · VIP
↓  תחזוקה · קנסות · בנייה
```

- שורת `↑` בירוק (`text-emerald-400`), שורת `↓` באדום (`text-rose-400`).
- מקסימום 3–4 פריטים בכל שורה, מופרדים בנקודה `·`. **טקסט קצר, לא משפטים.**
- מתחת לבלוק: קישור טקסט קטן `הסבר מלא ←` שפותח את **מילון המונחים** בערך הרלוונטי (סעיף 6), וסוגר זמנית את הסיור. בסגירת המילון הסיור חוזר לאותו שלב.

---

## 5. מבנה נתונים ו-API

### 5.1 שלבי הסיור

```js
// src/tour/tourSteps.js
export const OPENING_TOUR = [
  {
    id: 'welcome',
    target: '[data-tour="park-header"]',
    icon: '🏝️',
    title: 'ברוך הבא ל{{parkName}}',
    body: 'מהיום אתה המנהל, {{managerName}}. אלוהים יעזור למבקרים.',
    placement: 'bottom',
    advanceOn: 'next',
    spotlightPadding: 8,
    effects: null,            // אין בלוק ↑↓ בשלב הזה
    glossaryKey: null,
  },
  {
    id: 'money',
    target: '[data-tour="kpi-money"]',
    icon: '💰',
    title: 'הכסף',
    body: 'כל שקל נחשב. אם תרד למינוס — הפארק נסגר ואתה חוזר לעבוד אצל אבא.',
    placement: 'bottom',
    advanceOn: 'next',
    effects: {
      up:   ['כרטיסים', 'מזנונים', 'VIP'],
      down: ['תחזוקה', 'קנסות', 'בנייה'],
    },
    glossaryKey: 'money',     // מקשר לערך במילון (סעיף 6)
  },
  // ... שאר השלבים בסעיף 5.4
];
```

**שדות חובה:** `id`, `target`, `title`, `body`, `placement`, `advanceOn`.
**שדות אופציונליים:** `icon`, `effects`, `glossaryKey`, `spotlightPadding`, `condition`, `waitForTarget`, `bodyByDifficulty`.

- `condition: (gameState) => boolean` — אם מחזיר `false`, השלב מדולג בשקט. נדרש בעיקר בהפעלה מחדש באמצע משחק.
- `waitForTarget: true` — ממתין עד 3 שניות להופעת היעד ב-DOM (MutationObserver) לפני שמדלג.
- `bodyByDifficulty: { easy, normal, balagan }` — טקסט חלופי לפי רמת קושי; אם חסר, משתמשים ב-`body`.

### 5.2 API

```js
const tour = useTour();

tour.start('opening');            // מפעיל את הסיור הפותח מהתחלה
tour.showTip('tip-first-event');  // מציג טיפ בודד (מתעלם אם כבר נצפה או אם טיפים כבויים)
tour.stop();                      // סוגר
tour.reset();                     // מנקה את כל ה"נצפו"
tour.openGlossary('satisfaction');// פותח את המילון בערך מסוים
tour.isActive;                    // boolean — לשימוש להקפאת לוגיקת המשחק
```

### 5.3 שמירת מצב
- `localStorage`, מפתח יחיד: `waterpark_tour_state`
- מבנה: `{ version: 2, seenSteps: string[], toursCompleted: string[], tipsDisabled: boolean }`
- **`version`** — העלאת המספר בעדכון עתידי מאפסת את המצב כדי שהשחקן יראה תוכן חדש. יש לטפל בגרסה ישנה/פגומה ב-`try/catch` ולאתחל מחדש במקום לקרוס.
- `seenSteps` נשמר **פר-משחק-שמור** (טיפים חוזרים במשחק חדש). `tipsDisabled` — **גלובלי**.

### 5.4 תוכן הסיור הפותח — 5 שלבים

| # | `id` | יעד (`data-tour`) | כותרת | גוף | ↑ מעלה | ↓ מוריד |
|---|---|---|---|---|---|---|
| 1 | `welcome` | `park-header` | 🏝️ ברוך הבא ל{{parkName}} | מהיום אתה המנהל, {{managerName}}. אלוהים יעזור למבקרים. | — | — |
| 2 | `feed` | `feed` | 📰 הפיד — פה קורים החיים | כל חודש נופלים לפה מבזקים, קבוצת הוואטסאפ של השכונה, ביקורות גוגל, לחישות של הצוות והדוח החודשי. שום דבר לא נמחק. | — | — |
| 3 | `money` | `kpi-money` | 💰 הכסף | כל שקל נחשב. אם תרד למינוס — הפארק נסגר ואתה חוזר לעבוד אצל אבא. | כרטיסים · מזנונים · VIP | תחזוקה · קנסות · בנייה |
| 4 | `satisfaction` | `kpi-rating` | ⭐ שביעות רצון ודירוג | המבקרים מדרגים אותך בלי רחמים, בדיוק כמו בקבוצת הוואטסאפ של השכונה. | צל · מגוון מתקנים · חידושים | צפיפות · תקלות · תורים |
| 5 | `visitors` | `kpi-visitors` | 👥 מבקרים | באוגוסט הם באים בהמונים. בינואר תשמע צרצרים. תתכנן בהתאם. | עונה חמה · פרסום · דירוג גבוה | חורף · דירוג נמוך · ביקורות רעות |

**הערות מימוש לתוכן:**
- טקסטים תומכים בהחלפת משתנים בתבנית `{{parkName}}`, `{{managerName}}`, `{{money}}`, `{{difficulty}}` דרך פונקציית `interpolate(text, gameState)`. אם משתנה חסר — להחליף במחרוזת ריקה, **לא** להציג `{{...}}` למשתמש.
- בקושי 🔴 "יאללה בלאגן", שלב 2 מקבל `bodyByDifficulty.balagan`: *"התקציב שלך דק. מאוד. בהצלחה."*
- שלב 5 הוא האחרון — כפתור "יאללה, בוא נתחיל 🏊" שסוגר את הסיור ומתחיל את החודש הראשון.
- **`kpi-visitors` יושב בתוך הפאנל המתקפל** של שורת ה-KPI (`.ts-detail`, שמוגדר `max-height:0`). ההכרעה שנבחרה: **הצעד פותח את הפאנל בעצמו** לפני שהוא מצביע עליו — ולא מצביע במקומו על קלף הדוח בפיד. לשם כך נוסף למנוע ה-Tour הוק `prepare()` שרץ אחרי `condition()` ולפני חיפוש היעד:

  ```js
  { id:"visitors", target:'[data-tour="kpi-visitors"]',
    prepare:()=>openKpiPanel(), waitForTarget:true, ... }
  ```

  `openKpiPanel()` מרים את `G._kpiOpen` ומוסיף `.open` לפאנל, כך שהמצב נשמר גם אחרי `render()` נוסף. `waitForTarget:true` נותן לאנימציית הפתיחה (0.28s) להסתיים לפני מדידת המיקום.
- **אם `kpi-rating` ו-`kpi-visitors` יושבים באותה שורת KPI צמודה** — לוודא ש-`spotlightPadding` לא גורם לחורים לחפוף. במקרה כזה להוריד ל-4px.

---

## 6. מילון מונחים 📖

מסך/שיטס שנפתח מכפתור ❓ או מקישור "הסבר מלא ←" בבועות ובטיפים. **זה המקום שבו מוסבר לעומק מה מעלה ומה מוריד כל מדד.**

### 6.1 מבנה המסך
- Bottom Sheet מלא-מסך (או מודאל בדסקטופ), עם כותרת `📖 מילון מונחים` וכפתור סגירה.
- רשימת ערכים כאקורדיון — ערך אחד פתוח בכל רגע. פתיחה דרך `openGlossary(key)` גוללת לערך ופותחת אותו.
- כל ערך מכיל: **כותרת · משפט הגדרה (בהומור) · טבלת ↑ מעלה · טבלת ↓ מוריד · "הערת מנהל" (טיפ פרקטי אחד)**.
- שתי הטבלאות בעיצוב זהה, נבדלות רק בצבע (ירוק/אדום) ובאייקון.

### 6.2 מבנה נתונים

```js
// src/tour/glossary.js
export const GLOSSARY = [
  {
    key: 'satisfaction',
    icon: '😊',
    title: 'שביעות רצון',
    definition: 'ציון 0–100 שאומר כמה המבקרים אהבו את הביקור. יש לו זיכרון — חודש אחד גרוע לא הורס הכול, וחודש אחד טוב לא מציל.',
    note: 'שביעות רצון נמוכה בקיץ עולה הכי הרבה כסף, כי אז יש הכי הרבה מבקרים להפסיד.',
    up: [ { label: 'יחס מתקנים/מבקרים תקין', weight: 'עד +10' }, /* ... */ ],
    down: [ { label: 'צפיפות יתר', weight: 'עד -15' }, /* ... */ ],
  },
  // ...
];
```

> **חשוב:** אם קיימים במשחק קבועים של המשקלים — לייבא מהם ולגזור את `weight` דינאמית, במקום להעתיק מספרים.

### 6.3 תוכן המילון

#### 😊 שביעות רצון (0–100)
*הגדרה:* ציון שאומר כמה המבקרים אהבו את הביקור. יש לו זיכרון — 70% מהחודש הקודם + 30% ממה שקרה עכשיו.

| ↑ מה מעלה | כמה |
|---|---|
| יחס מתקנים/מבקרים תקין (לא צפוף) | עד +10 |
| מתקנים חדשים או משודרגים לאחרונה | עד +10 |
| מגוון מתקנים (גם ילדים וגם מבוגרים) | +5 |
| מספיק צל וישיבה | +5 |
| הסעדה איכותית ומגוונת | +5 |
| הדירוג עלה ביחס לחודש הקודם | +3 |

| ↓ מה מוריד | כמה |
|---|---|
| אירוע שלילי שלא טופל | עד -20 |
| צפיפות יתר | עד -15 |
| מתקן מקולקל או סגור בלי הודעה | -10 |
| הזנחת תחזוקה מצטברת (רגיל ומעלה) | עד -10 |
| אין צל/ישיבה בשיא הקיץ | -8 |
| תור ארוך | -5 |
| מחיר גבוה מדי ביחס לאיכות | -5 |

*הערת מנהל:* בגלל הזיכרון, כדאי לתקן מוקדם. גרירה של שלושה חודשים גרועים לוקחת חצי עונה לתקן.

#### 💰 כסף
*הגדרה:* היתרה בקופה. מתחת לאפס — במצב "שרוד עונה" זה סוף המשחק.

| ↑ מה מעלה | ↓ מה מוריד |
|---|---|
| מכירת כרטיסים (מבקרים × מחיר) | תחזוקה חודשית של כל מתקן ומזנון |
| הסעדה (פלאפל, גלידה, מסעדה) | קנסות מאירועים שליליים |
| הכנסות VIP (ג'קוזי, פרגולה) | עלויות בנייה ושדרוג |
| מענקים ותרומות (אירוע חיובי) | קמפיינים פרסומיים |
| — | תשלום להאצת בנייה |

*הערת מנהל:* התחזוקה יורדת גם בחודשי החורף שבהם אין מבקרים. תכנן רזרבה לינואר–פברואר.

#### 👥 מבקרים
*הגדרה:* כמה אנשים הגיעו החודש. הנוסחה: `ביקוש בסיס × מכפיל עונה × (1 + פרסום) × (שביעות רצון / 100)`.

| ↑ מה מעלה | ↓ מה מוריד |
|---|---|
| עונה חמה (יולי ×3.0, אוגוסט ×3.2) | חורף (ינואר/דצמבר ×0.1) |
| חגים — פסח, סוכות, ראש השנה | דירוג נמוך |
| קמפיין פרסומי פעיל (+5% עד +20%) | ביקורת רעה או וידאו ויראלי שלילי |
| שביעות רצון גבוהה | מתקנים סגורים |
| אירועים חיוביים (סלב, כתבה) | — |

*הערת מנהל:* שביעות רצון מכפילה את הכול. פארק מוזנח נפגע הכי חזק דווקא באוגוסט — שם ההפסד האמיתי.

#### ⭐ דירוג כוכבים
*הגדרה:* תרגום ישיר של שביעות הרצון לכוכבים. לא משתנה בנפרד — רק דרכה.

| טווח | דירוג |
|---|---|
| 0–20 | ⭐ "לא הייתי חוזר גם אם ישלמו לי" |
| 21–40 | ⭐⭐ "היה בסדר, המים היו רטובים" |
| 41–60 | ⭐⭐⭐ "כיף משפחתי סטנדרטי" |
| 61–80 | ⭐⭐⭐⭐ "תיק ותסע, שווה!" |
| 81–100 | ⭐⭐⭐⭐⭐ "הפארק הכי טוב אחרי הכינרת" |

#### 🏊 קיבולת
*הגדרה:* כמה מבקרים ביום הפארק מסוגל לקלוט. סכום הקיבולת של כל המתקנים הפעילים.

*↑ מעלה:* בניית מתקנים חדשים · סיום שדרוג · הרחבת שטח.
*↓ מוריד:* מתקן שנסגר לתיקון · מתקן בבנייה (עדיין לא סופר).

*הערת מנהל:* כשמבקרים > קיבולת — זו הצפיפות שמורידה שביעות רצון. באוגוסט תבדוק את זה לפני שתשקיע בפרסום.

#### 🔧 תחזוקה
*הגדרה:* עלות חודשית קבועה לכל מתקן. כוללת בתוכה גם את שכר העובדים.

| רמת קושי | התנהגות |
|---|---|
| 🟢 קליל | כבויה לגמרי |
| 🟡 רגיל | פעילה לפי הטבלאות |
| 🔴 יאללה בלאגן | פעילה + מתקנים ישנים מייקרים ומעלים סיכון תקלה |

#### 📅 עונתיות
*הגדרה:* מכפיל ביקוש חודשי. הגורם החזק ביותר במשחק.

טבלת המכפילים המלאה (ינואר ×0.1 … אוגוסט ×3.2). **לייבא מטבלת העונתיות של המשחק, לא לשכפל.**

*הערת מנהל:* בנייה של 3 חודשים שמתחילה במאי מוכנה לאוגוסט. שמתחילה ביולי — מפספסת את כל העונה.

#### 🛡️ אבטחה
*הגדרה:* שדרוג רוחבי שמוריד סיכוני אירועים.

*↑ נותן:* פחות קטטות וגניבות · פחות "ילד נעדר" · הגנה בביקורת משרד הבריאות · שקט נפשי להורים.
*↓ עולה:* התקנה חד-פעמית + עלות חודשית קבועה.

---

## 7. טיפים בהקשר

| `trigger` | יעד (`data-tour`) | תוכן | `glossaryKey` |
|---|---|---|---|
| `FIRST_BUILD_SCREEN_OPEN` | `build-tabs` | 🔨 "פה בונים הכול. אפור = אין תקציב או אין שטח. בנייה לוקחת חודשים — תכנן מראש." | — |
| `FIRST_CONSTRUCTION_START` | `construction-progress` | 🚧 "בבנייה. אפשר להאיץ בתשלום, או פשוט לנשום ולחכות." | `capacity` |
| `FIRST_RANDOM_EVENT` | `event-sheet` | 📰 "כל חודש קורה משהו. לכל בחירה יש מחיר — ולפעמים גם לחוסר בחירה." | — |
| `FIRST_MONTHLY_REPORT` | `report-financials` | 📊 "מכאן הכסף בא, לכאן הוא הלך." | `money` |
| `SATISFACTION_BELOW_40` | `kpi-rating` | 😐 "המבקרים מתלוננים. צל, מקום וישיבה — זה בדרך כלל הפתרון." | `satisfaction` |
| `NEGATIVE_BALANCE` | `kpi-money` | 🔴 "אתה במינוס. עוד חודש כזה ונגמר הסיפור." | `money` |
| `RUTI_ON_YOUR_CASE` | `ruti-chip` | 🕵️‍♀️ "רותי עש שמה עלייך עין. יש לה זיכרון ומד חום — שביעות רצון נמוכה, מחיר גבוה ותשובות מתחכמות מחממות אותה, תיקונים מקררים. בשלב 4 היא כבר עולה כסף." | — |
| `FIRST_NEXT_MONTH` | `next-month-btn` | ➡️ "כשסיימת — לוחץ פה והחודש מתקדם. אין חזרה אחורה." | — |

**כללים:**
- טיפ מוצג **פעם אחת** למשחק שמור.
- **לא יותר מטיפ אחד בחודש משחק.** אם שני טריגרים נדלקים באותו חודש — הראשון מוצג, השני נכנס לתור FIFO ומוצג בחודש הבא (אם עדיין רלוונטי).
- טיפ משתמש **באותו רכיב בועה** של הסיור, בלי מונה שלבים ובלי "הקודם" — רק "הבנתי 👍" ו"הסבר מלא ←" אם יש `glossaryKey`.

---

## 8. אינטגרציה עם המשחק

1. **הקפאה:** כש-`tour.isActive === true` — לחסום `advanceMonth()`, גלגול אירועים וטיימרים. **לא** לחסום רינדור.
2. **חסימת קליקים:** ה-Overlay חוסם `pointer-events` על כל המסך (סעיף 3).
3. **נגישות:** לבועה `role="dialog"` + `aria-live="polite"`, מיקוד עובר לכפתור "הבא" בכל שלב, focus trap בתוך הבועה, וב-`Esc` — דיאלוג דילוג.
4. **הוספת `data-tour`:** לעבור על הרשימה בסעיף 8.1 ולהוסיף את התכונה. **לא לשנות דבר אחר** באלמנטים האלה.
5. **z-index:** Overlay `9998`, בועה `9999`, מילון `10000` (מעליהם). לוודא שאין התנגשות עם ה-Bottom Sheet הקיים של האירועים.

### 8.1 רשימת ה-`data-tour` הנדרשים

> **עודכן בעקבות עיצוב הפיד.** הדשבורד נבנה מחדש (`topstrip` · `dilslot` · `ftabs` · `feed` · `bottombar`),
> וכל העוגנים הועברו איתו. הטבלה מתארת את המצב הנוכחי.

| ערך | האלמנט במשחק |
|---|---|
| `park-header` | שם הפארק בשורה העליונה של `.topstrip` |
| `kpi-money` | צ'יפ 💰 בשורת הסטטוסים של `.topstrip` (לחיצה פותחת את המילון) |
| `kpi-rating` | צ'יפ ⭐ בשורת הסטטוסים של `.topstrip` |
| `kpi-visitors` | תא 👥 בתוך הפאנל המתקפל `.ts-detail` — ראו הערת המימוש בסעיף 5.4 |
| `ruti-chip` | צ'יפ 🕵️‍♀️ של רותי עש ב-`.topstrip` (מוצג רק משלב 1 ומעלה) |
| `feed` | אזור הפיד הגליל `.feed` |
| `nav-build` | כפתור 🏗️ בסרגל התחתון |
| `build-tabs` | שורת הטאבים במסך הבנייה (ללא שינוי) |
| `construction-progress` | פס ההתקדמות של פריט בבנייה (ללא שינוי) |
| `event-sheet` | שאלת הדילמה בסלוט הדביק `.dilslot` (הדילמה כבר לא Bottom Sheet) |
| `report-financials` | כפתור "💵 פירוט כספי" בתוך קלף הדוח החודשי בפיד |
| `next-month-btn` | כפתור "➡️ סגור חודש" בסרגל התחתון |
| `help-btn` | כפתור ה-❓ בשורה העליונה של `.topstrip` |

**טריגרים שהזיזו מקום:**
- `FIRST_RANDOM_EVENT` נורה מ-`renderDilemmaSlot()` במקום מ-`renderDilemmaOverlay()` (שנמחק).
- `FIRST_MONTHLY_REPORT` נורה מ-`renderReportCard()` ברגע שקלף הדוח נכנס לפיד, במקום מה-overlay (שנמחק).
- `RUTI_ON_YOUR_CASE` נורה מ-`renderDashboard()` בפעם הראשונה ש-`G.ruti.stage >= 2`.

---

## 9. מקרי קצה

| מקרה | התנהגות נדרשת |
|---|---|
| היעד לא קיים ב-DOM | עם `waitForTarget: true` — המתנה עד 3 שניות (MutationObserver). אחרת — **דילוג שקט** לשלב הבא + `console.warn`. **בשום מקרה לא לקרוס ולא להציג בועה ריקה.** |
| היעד מוסתר (`display:none` / `visibility:hidden` / רוחב או גובה 0) | כמו למעלה — דילוג שקט. |
| שינוי גודל חלון / סיבוב מכשיר | חישוב מחדש (`ResizeObserver` + `resize`, debounce 100ms). |
| היעד זז בזמן שהשלב מוצג | מעקב ב-`requestAnimationFrame` כל עוד השלב פעיל. |
| רענון דף באמצע הסיור | הסיור **לא** מתחדש אוטומטית. שלבים שנצפו מסומנים; ההמשך יופיע רק בהפעלה מפורשת מ-❓. |
| הפעלה מחדש מ-❓ באמצע משחק | שלבים שדורשים מצב לא-קיים מדולגים דרך `condition`. |
| כל 4 השלבים דולגו (יעדים חסרים) | לסגור את הסיור בשקט בלי להציג overlay ריק. |
| מסך קטן מאוד (<360px) | Bottom Sheet בלבד. |
| `localStorage` חסום/מלא | לעבוד מזיכרון בלבד (`try/catch`), בלי לקרוס. |
| השחקן פותח מילון מתוך בועה | הסיור נעצר זמנית; בסגירת המילון חוזר לאותו שלב בדיוק. |

---

## 10. קריטריוני קבלה (QA)

- [ ] הסיור נפתח אוטומטית מיד אחרי מסך ההגדרות במשחק חדש — ורק אז.
- [ ] בדיוק 4 שלבים, כולם מתקדמים ב"הבא".
- [ ] בכל שלב: חור מואר, טבעת פועמת, חץ שמצביע נכון, בועה קריאה.
- [ ] המעבר בין שלבים חלק (זרקור זוחל, לא קופץ).
- [ ] בלוק ↑/↓ מוצג בשלבים 2–4 בצבעים הנכונים.
- [ ] "הסבר מלא ←" פותח את המילון בערך הנכון, וסגירתו מחזירה לאותו שלב.
- [ ] "דלג על ההדרכה" עובד מכל שלב, מבקש אישור, וסוגר הכול.
- [ ] כפתור ❓ מפעיל את הסיור מחדש גם בחודש 9, ופותח את המילון.
- [ ] כל ערכי המילון נפתחים, מציגים ↑ ו-↓ ואת הערת המנהל.
- [ ] טיפ בהקשר מופיע פעם אחת בלבד ולא חוזר; לא יותר מאחד בחודש.
- [ ] המשחק לא מתקדם בזמן שהסיור פתוח.
- [ ] בנייד (390×844 ו-360×640) הבועה לא נחתכת ולא מסתירה את היעד.
- [ ] RTL תקין בכל השלבים (מונה משמאל, כפתורים מימין, חץ נכון).
- [ ] `{{parkName}}` ו-`{{managerName}}` מוחלפים בערכים אמיתיים בכל מקום.
- [ ] מחיקת `localStorage` מחזירה למצב התחלתי.
- [ ] יעד חסר לא מפיל את המשחק.

---

## 11. סדר עבודה מומלץ

1. `TourProvider` + `useTour` + `TourOverlay` — Engine בלבד, עם שלב דמו אחד.
2. הוספת `data-tour` לכל האלמנטים בסעיף 8.1.
3. מילוי `tourSteps.js` ב-4 השלבים + בלוק ה-↑/↓.
4. חיבור ההפעלה האוטומטית בסיום מסך ההגדרות + כפתור ❓ בהדר.
5. מסך המילון (`glossary.js` + קומפוננטה) וחיבור "הסבר מלא ←".
6. טריגרים לטיפים בהקשר + תור FIFO.
7. ליטוש: אנימציות, Bottom Sheet, נגישות, מקרי קצה.
8. מעבר על צ'קליסט סעיף 10.

**לבצע שלב-שלב ולוודא שהמשחק עדיין עובד אחרי כל שלב.**
